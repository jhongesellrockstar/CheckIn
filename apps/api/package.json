{
  "name": "@checkin/api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port ${PORT:-8000}",
    "build": "echo 'No build step'",
    "lint": "ruff check . && black --check . && mypy ."
  }
}
